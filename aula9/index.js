// Diferença entre Var e Let
var nome = 'Matheus';
var nome = 'Gomes';

console.log(nome); // com o Var, da pra redeclarar a variável utilizando o var antes

outro_nome = 'matheus';
console.log(outro_nome); //Não fazer isso, pode transformar em global e afetar outras variáveis
